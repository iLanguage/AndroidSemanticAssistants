package info.semanticsoftware.semassist.server.rest.resource;

/*import java.util.List;

import info.semanticsoftware.semassist.server.GateRuntimeParameter;
import info.semanticsoftware.semassist.server.GateRuntimeParameterArray;
import info.semanticsoftware.semassist.server.ServiceInfoForClient;
import info.semanticsoftware.semassist.server.rest.model.RTParamModel;
import info.semanticsoftware.semassist.server.rest.model.ServiceModel;

import org.restlet.data.MediaType;
import org.restlet.representation.Representation;
import org.restlet.representation.StringRepresentation;
import org.restlet.resource.Get;*/
import org.restlet.resource.ServerResource;

public class RTParamsResource extends ServerResource{
	
	/*@Get("xml")
	public Representation getXML() {
	        String xml = new RTParamModel().getAllXML();
			Representation representation = new StringRepresentation(xml, MediaType.APPLICATION_XML);
			return representation;
	}*/

}
